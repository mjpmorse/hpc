The tarball contains all the code for the homework.

Problem 1: contains the queue and the .sh file

Problem 2: contains python code

Problem 3: contains the my .f90 source code. and my make files. To make the optimized vs un optimized data change FFLAG in make file to include -O3. The fortran code will read in a file name from terminal to name the data file.  There is also a python script to make a plot.

Problem 4: contains my .f90 code and the make file to make the mkl vs nonmkl versions. The program will recieve data file name passed from terminal 
Problem 5: same as 4
  
